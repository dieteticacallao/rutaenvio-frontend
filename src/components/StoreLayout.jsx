// Placeholder para el layout del panel de tienda (STORE_ADMIN).
// Se va a implementar cuando se desarrollen las paginas del panel de tienda.
export default function StoreLayout({ children }) {
  return <div>{children}</div>
}
